<% include partials/header %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Crime Rate Prediction</title>
    
    <!-- ✅ Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- ✅ Font Awesome for Icons -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <!-- ✅ Custom CSS -->
    <link rel="stylesheet" href="/style.css">
    
    <!-- ✅ Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
        /* 🔹 Glassmorphism Effect */
        .glassmorphism {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.3);
        }

        /* 🔹 Hero Section */
        #landing-header {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('background.jpg') center/cover;
            height: 100vh;
        }

        /* 🔹 Stylish Buttons */
        .btn-custom {
            transition: 0.3s ease-in-out;
        }
        .btn-custom:hover {
            transform: scale(1.1);
        }

        /* 🔹 Team Section */
        .team-card {
            padding: 15px;
            border-radius: 8px;
            background: rgba(240, 240, 240, 0.9);
            transition: transform 0.3s ease-in-out;
        }
        .team-card:hover {
            transform: scale(1.05);
        }
    </style>

</head>

<body class="container-fluid text-center">
    
    <!-- 🔹 Hero Section -->
    <div id="landing-header" class="d-flex flex-column justify-content-center align-items-center text-white">
        
        <div class="glassmorphism text-center">
            <h1 class="display-3 fw-bold" style="font-family: 'Playfair Display', serif;">
                Crime Rate Prediction
            </h1>

            <h3 class="fs-4 mt-3" style="font-family: 'Poppins', sans-serif; font-weight: 300;">
                Because you must look before you leave!
            </h3>

            <!-- Call-to-Action Buttons -->
            <div class="mt-4">
                <a href="/login" class="btn btn-outline-light btn-lg mx-2 shadow-sm btn-custom">
                    <i class="fas fa-sign-in-alt"></i> Login
                </a>
                <a href="/signup" class="btn btn-light btn-lg mx-2 shadow-sm btn-custom">
                    <i class="fas fa-user-plus"></i> Sign Up
                </a>
            </div>
        </div>
    </div>

    <!-- 🔹 Team Section -->
    <div class="container my-5">
        <h2 class="text-center mb-5" style="font-family: 'Playfair Display', serif; color: black;">
            Meet Our Team
        </h2>
    
        <div class="row justify-content-center gap-4">
            <div class="col-md-4 col-lg-3 text-center team-card">
                <h5 class="fw-bold text-dark">Vishal Kumar Kushwaha</h5>
            </div>
    
            <div class="col-md-4 col-lg-3 text-center team-card">
                <h5 class="fw-bold text-dark">Utkarsh Bhadauriya</h5>
            </div>
    
            <div class="col-md-4 col-lg-3 text-center team-card">
                <h5 class="fw-bold text-dark">Durgesh Rajbhar</h5>
            </div>
    
            <div class="col-md-4 col-lg-3 text-center team-card">
                <h5 class="fw-bold text-dark">Pravesh Bajpai</h5>
            </div>
    
            <div class="col-md-4 col-lg-3 text-center team-card">
                <h5 class="fw-bold text-dark">Avee Kumar</h5>
            </div>
        </div>
    </div>

</body>
</html>
